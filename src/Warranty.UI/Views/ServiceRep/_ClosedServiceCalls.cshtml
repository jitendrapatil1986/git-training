@using Warranty.UI.Core.Helpers
@model Warranty.Core.Features.RepServiceCalls.ServiceRepServiceCallsModel

@foreach (var line in Model.ClosedServiceCalls)
{
    <div class="service-call-line">
        <div class="col-md-1">
            <small class="text-muted">@line.CallNumber</small><br/>@Format.YearsWithinWarranty(line.YearsWithinWarranty, line.WarrantyStartDate)
        </div>
        <div class="col-md-3">
            <div class="col-md-1 hidden-sm hidden-xs">
                @Format.NumberOfLineItems(line.NumberOfLineItems)
            </div>
            <div class="col-md-10">
                <a>@line.HomeownerName</a><br />@line.Address
            </div>
        </div>
        <div class="col-md-2">@Format.Escalated(line.IsEscalated, line.EscalationReason, line.EscalationDate)</div>
        <div class="col-md-1">@Format.DaysOpenedFor(line.DaysOpenedFor, line.CreatedDate, line.CompletionDate)</div>
        <div class="col-md-1">@Format.SpecialProject(line.IsSpecialProject)</div>
        <div class="clearfix"></div>
    </div>
}